apiVersion: "camel.apache.org/v1"
kind: "Kamelet"
metadata:
  name: "setoriginalbody-action"
  labels:
    camel.apache.org/kamelet.type: "action"
spec:
  definition:
    title: "setoriginalbody action"
    description: "Sets the original body of a exchange"
    type: "object"
    properties:
      path:
        title: "Path"
        description: "Path"
        type: "string"
      in:
        title: "Source Endpoint"
        type: "string"
        default: "kamelet:source"
      out:
        title: "Sink Endpoint"
        type: "string"
        default: "kamelet:sink"
      routeId:
        title: "Route ID"
        type: "string"
      routeConfigurationId:
        title: "Route Configuration ID"
        type: "string"
        default: "0"
  dependencies:
  - "camel:kamelet"
  template:
    route:
      routeConfigurationId: "{{routeConfigurationId}}"
      from:
        uri: "{{in}}"
        steps:
        - step:
            id: "{{routeId}}"
            steps:
            - setBody:
                simple: "${originalBody}"
            - to:
                uri: "{{out}}"
