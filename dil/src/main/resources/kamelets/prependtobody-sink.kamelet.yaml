apiVersion: camel.apache.org/v1alpha1
kind: Kamelet
metadata:
  name: prependtobody-sink
  labels:
    camel.apache.org/kamelet.type: "sink"
spec:
  definition:
    title: "prependtobody action"
    description: |-
      Prepend to message body
    type: object
    properties:
      routeconfiguration_id:
        type: string
        default: "0"
      path:
        title: Path
        description: Path
        type: string
  dependencies:
    - "camel:kamelet"
  template:
    route:
      route-configuration-id: "{{routeconfiguration_id}}"
      from:
        uri: "kamelet:source"
        steps:
          - transform:
              body:
                prepend: "{{path}}"
          - to:
              uri: "kamelet:sink"