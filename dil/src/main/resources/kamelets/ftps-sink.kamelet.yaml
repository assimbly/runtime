apiVersion: "camel.apache.org/v1"
kind: "Kamelet"
metadata:
  name: "ftps-sink"
  labels:
    camel.apache.org/kamelet.type: "sink"
spec:
  definition:
    title: "ftps sink"
    description: "Sends messages to a FTPS directory"
    type: "object"
    properties:
      path:
        title: "path"
        description: "path of the URI"
        type: "string"
      options:
        title: "options (query parameterS)"
        description: "URI query parameters (Endpoint options)"
        type: "string"
      in:
        title: "Source Endpoint"
        type: "string"
        default: "kamelet:source"
      out:
        title: "Sink Endpoint"
        type: "string"
        default: "kamelet:sink"
      routeId:
        title: "Route ID"
        type: "string"
      routeConfigurationId:
        title: "Route Configuration ID"
        type: "string"
        default: "0"
  dependencies:
  - "camel:kamelet"
  template:
    route:
      routeConfigurationId: "{{routeConfigurationId}}"
      from:
        uri: "{{in}}"
        steps:
        - step:
            id: "{{routeId}}"
            steps:
            - toD:
                uri: "ftps://{{path}}?{{?options}}"