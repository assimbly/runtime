apiVersion: camel.apache.org/v1alpha1
kind: Kamelet
metadata:
  name: setproperty-action
  labels:
    camel.apache.org/kamelet.type: "action"
spec:
  definition:
    title: "setproperty action"
    description: |-
      to do
    type: object
    properties:
      routeconfiguration_id:
        type: string
        default: "0"
      path:
          title: 
          description: .
          type: string
      value:
          title: 
          description: .
          type: string
      language:
          title: .
          description: .
          type: string
          default: constant
  dependencies:
    - "camel:kamelet"
  template:
    route:
      route-configuration-id: "{{routeconfiguration_id}}"
      from:
        uri: "kamelet:source"
        steps:
          - to:
              uri: "kamelet:sink"