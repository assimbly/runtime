apiVersion: "camel.apache.org/v1"
kind: "Kamelet"
metadata:
<<<<<<<< HEAD:dil/src/main/resources/kamelets/langchain4j-web-search-action.kamelet.yaml
  name: "langchain4j-web-search-action"
========
  name: "langchain4jagent-action"
>>>>>>>> next:dil/src/main/resources/kamelets/langchain4jagent-action.kamelet.yaml
  labels:
    camel.apache.org/kamelet.type: "action"

spec:
  definition:
<<<<<<<< HEAD:dil/src/main/resources/kamelets/langchain4j-web-search-action.kamelet.yaml
    title: "langchain4j-web-search action"
    description: "Web Search component for LangChain4j"
========
    title: "langchain4jagent action"
    description: "agent component for langchain4j"
>>>>>>>> next:dil/src/main/resources/kamelets/langchain4jagent-action.kamelet.yaml
    type: "object"
    properties:
      in:
        title: "Source Endpoint"
        type: "string"
        default: "kamelet:source"
      out:
        title: "Sink Endpoint"
        type: "string"
        default: "kamelet:sink"
      routeId:
        title: "Route ID"
        type: "string"
      routeConfigurationId:
        title: "Route Configuration ID"
        type: "string"
        default: "0"
      searchId:
        title: "Search ID"
        type: "string"
<<<<<<<< HEAD:dil/src/main/resources/kamelets/langchain4j-web-search-action.kamelet.yaml
        default: "search"
      webSearchEngine:
        title: "Web Search Engine Bean Name"
        description: "Name of the WebSearchEngine bean in the Camel registry (e.g. #webSearchEngine)"
        type: "string"
        default: "#webSearchEngine"
      maxResults:
        title: "Max Results"
        description: "Maximum number of search results to return"
        type: "integer"
        default: 1
      resultType:
        title: "Result Type"
        description: "Result type (CONTENT, SNIPPET, or LANGCHAIN4J_WEB_SEARCH_ORGANIC_RESULT)"
        type: "string"
        default: "SNIPPET"

  dependencies:
    - "camel:kamelet"
    - "camel:langchain4j-web-search"
========
        default: "geminiAgent"
      memoryId:
        title: "Memory ID"
        description: "The memory ID to identify the conversation session. Can be a Camel Simple expression."
        type: "string"
        default: ""

  dependencies:
    - "camel:kamelet"
>>>>>>>> next:dil/src/main/resources/kamelets/langchain4jagent-action.kamelet.yaml

  template:
    route:
      routeConfigurationId: "{{routeConfigurationId}}"
      from:
        uri: "{{in}}"
        steps:
          - step:
              id: "{{routeId}}"
              steps:
                - convertBodyTo:
                    type: String
<<<<<<<< HEAD:dil/src/main/resources/kamelets/langchain4j-web-search-action.kamelet.yaml
                - to:
                    uri: "langchain4j-web-search:{{searchId}}?webSearchEngine={{webSearchEngine}}&maxResults={{maxResults}}&resultType={{resultType}}"
========
                - choice:
                    when:
                      - simple: "'{{?memoryId}}' != ''"
                        steps:
                          - setHeader:
                              name: "CamelLangChain4jAgentMemoryId"
                              simple: "{{memoryId}}"
                    otherwise:
                      steps:
                        - setHeader:
                            name: "CamelLangChain4jAgentMemoryId"
                            simple: "${exchangeId}"
                - to:
                    uri: "langchain4j-agent://{{routeId}}?agent=#{{agent}}"
>>>>>>>> next:dil/src/main/resources/kamelets/langchain4jagent-action.kamelet.yaml
          - to:
              uri: "{{out}}"
