apiVersion: camel.apache.org/v1
kind: Kamelet
metadata:
  name: headerstopdf-action
  labels:
    camel.apache.org/kamelet.type: "action"
spec:
  definition:
    title: "Headers to PDF action"
    description: |-
      Add message headers as Adobe elements to a PDF template
    type: object
    properties:
      path:
          title: Path
          description: Path
          type: string
      options:
          title: Options
          description: Options
          type: string
  dependencies:
    - "camel:kamelet"
  template:
    route:
      from:
        uri: "kamelet:source"
        steps:
          - to:
              uri: "headerstopdf://{{path}}?{{options}}"
          - to:
              uri: "kamelet:sink"