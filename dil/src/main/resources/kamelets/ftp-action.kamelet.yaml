apiVersion: camel.apache.org/v1
kind: Kamelet
metadata:
  name: ftp-action
  labels:
    camel.apache.org/kamelet.type: "action"
spec:
  definition:
    title: "ftp action"
    description: |-
      Send messages to a FTP directory
    type: object
    properties:
      path:
        title: path
        description: path of component
        type: string
      options:
        title: options (query parameterS)
        description: Endpoint URI
        type: string
  dependencies:
    - "camel:kamelet"
  template:
    route:
      from:
        uri: "kamelet:source"
        steps:
          - toD:
              uri: "ftp:{{path}}?{{?options}}"
          - to:
              uri: "kamelet:sink"