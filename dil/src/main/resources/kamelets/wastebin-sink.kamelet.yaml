apiVersion: "camel.apache.org/v1"
kind: "Kamelet"
metadata:
  name: "wastebin-sink"
  labels:
    camel.apache.org/kamelet.type: "sink"
spec:
  definition:
    title: "wastebin sink"
    description: "Sends a message into nowhere"
    type: "object"
    properties:
      in:
        title: "Source Endpoint"
        type: "string"
        default: "kamelet:source"
      out:
        title: "Sink Endpoint"
        type: "string"
        default: "kamelet:sink"
      routeId:
        title: "Route ID"
        type: "string"
      routeConfigurationId:
        title: "Route Configuration ID"
        type: "string"
        default: "0"
  dependencies:
  - "camel:kamelet"
  template:
    route:
      from:
        uri: "{{in}}"
        steps:
        - step:
            id: "{{routeId}}"
            steps:
            - to:
                uri: "mock:wastebin"
      routeConfigurationId: "{{routeConfigurationId}}"
