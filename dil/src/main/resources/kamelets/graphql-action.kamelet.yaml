apiVersion: "camel.apache.org/v1"
kind: "Kamelet"
metadata:
  name: "graphql-action"
  labels:
    camel.apache.org/kamelet.type: "action"
spec:
  definition:
    title: "GraphQL Action"
    description: Forward data to a GraphQL endpoint.
    required:
      - url
    type: object
    properties:
      url:
        title: GraphQL Server URL
        description: The URL to which you want to send data.
        type: string
        pattern: "^(http|https)://.*"
      accessToken:
        title: GraphQL Access Token
        description: The access Token to use to access GraphQL server
        type: string
        format: password
        x-descriptors:
          - urn:camel:group:credentials
      query:
        title: GraphQL Query
        description: If defined the graphql query (otherwise it takes the body as query)
        type: string
      in:
        title: "Source Endpoint"
        type: "string"
        default: "kamelet:source"
      out:
        title: "Sink Endpoint"
        type: "string"
        default: "kamelet:sink"
      sink:
        title: "Sink"
        type: "boolean"
        default: false
      routeId:
        title: "Route ID"
        type: "string"
      stepId:
        title: "Step ID"
        type: "string"
      routeConfigurationId:
        title: "Route Configuration ID"
        type: "string"
        default: "0"
  dependencies:
    - "camel:graphql"
    - "camel:kamelet"
    - "camel:core"
  template:
    route:
      routeConfigurationId: "{{routeConfigurationId}}"
      from:
        uri: "{{in}}"
        steps:
          - step:
              id: "step-{{stepId}}"
              steps:
              - removeHeader:
                  name: "Accept-Encoding"
              - to:
                  uri: "graphql://{{url}}"
                  parameters:
                    accessToken: "{{?accessToken}}"
                    query: "{{?query}}"
              - to:
                  disabled: "{{sink}}"
                  uri: "{{out}}"