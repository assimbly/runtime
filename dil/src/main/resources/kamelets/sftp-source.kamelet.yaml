apiVersion: camel.apache.org/v1
kind: Kamelet
metadata:
  name: sftp-source
  labels:
    camel.apache.org/kamelet.type: "source"
spec:
  definition:
    title: "sftp source"
    description: |-
      to do
    type: object
    properties:
      uri:
          title: Uri
          description: Uri
          type: string
  dependencies:
    - "camel:kamelet"
  template:
    route:
      from:
        uri: "{{uri}}"
        steps:
          - to:
              uri: "kamelet:sink"